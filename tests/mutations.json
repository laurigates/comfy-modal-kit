{
  "command": ["bun", "run", "test"],
  "reporter": "vitest-json",
  "mutations": [
    {
      "label": "hub-button election disabled (every pack would claim its own button)",
      "file": "src/hub.ts",
      "before": "if (kit.hubLauncherInstalled) return {};",
      "after": "if (false) return {};"
    },
    {
      "label": "registry replace becomes append (a re-registering pack duplicates its row)",
      "file": "src/hub-registry.ts",
      "before": "list.splice(i, 1, entry);",
      "after": "list.push(entry);"
    },
    {
      "label": "chooser priority ordering removed (both registries share one comparator)",
      "file": "src/hub-registry.ts",
      "before": "  return (b.priority ?? 0) - (a.priority ?? 0);",
      "after": "  return 0;"
    },
    {
      "label": "hubEntries backfill removed (the ADR-0003 undefined.findIndex crash)",
      "file": "src/kit-global.ts",
      "before": "if (!kit.hubEntries) kit.hubEntries = [];",
      "after": "if (false) kit.hubEntries = [];"
    },
    {
      "label": "close-before-act removed (Settings would open behind the z-9998 scrim)",
      "file": "src/hub.ts",
      "before": "controller.close(); // synchronous DOM detach — modal-shell.ts:333-335",
      "after": "void 0; // synchronous DOM detach — modal-shell.ts:333-335"
    },
    {
      "label": "single-entry short-circuit removed (a lone tool would cost an extra tap)",
      "file": "src/hub.ts",
      "before": "if (entries.length === 1) {",
      "after": "if (false) {"
    },
    {
      "label": "safe-view: token matching becomes SUBSTRING matching (blurs assets/ and classic.png)",
      "file": "src/safe-view.ts",
      "before": "if (haystack.has(kw)) return true;",
      "after": "if ([...haystack].some((t) => t.includes(kw))) return true;"
    },
    {
      "label": "safe-view: an unscanned file fails OPEN (shows what the prompt tier was meant to hide)",
      "file": "src/safe-view.ts",
      "before": "if (target.promptMatch === \"unscanned\") return true;",
      "after": "if (false) return true;"
    },
    {
      "label": "safe-view: a deliberately cleared keyword box snaps back to the default (unclearable)",
      "file": "src/safe-view.ts",
      "before": "keywords: rawKeywords === undefined ? SAFE_VIEW_DEFAULTS.keywords : parseKeywords(rawKeywords),",
      "after": "keywords: !rawKeywords ? SAFE_VIEW_DEFAULTS.keywords : parseKeywords(rawKeywords),"
    },
    {
      "label": "safe-view: the blur class is never applied (thumbnails render in the clear)",
      "file": "src/safe-view.ts",
      "before": "el.classList.toggle(SAFE_VIEW_BLUR_CLASS, blurred);",
      "after": "void blurred;"
    },
    {
      "label": "safe-view: the title is BLANKED instead of removed (name leaks via native tooltip)",
      "file": "src/safe-view.ts",
      "before": "el.removeAttribute(\"title\");",
      "after": "el.setAttribute(\"title\", \"\");"
    },
    {
      "label": "safe-view: the reveal tap stops suppressing the card's own click (reveal also opens)",
      "file": "src/safe-view.ts",
      "before": "e.stopPropagation();",
      "after": "void 0;"
    },
    {
      "label": "hub toggles: registry replace becomes append (both packs => two Safe View rows)",
      "file": "src/hub-registry.ts",
      "before": "list.splice(i, 1, toggle);",
      "after": "list.push(toggle);"
    },
    {
      "label": "hub toggles: the row stops flipping (set is no longer the negation of get)",
      "file": "src/hub.ts",
      "before": "toggle.set(!toggle.get());",
      "after": "toggle.set(true);"
    },
    {
      "label": "scroll-restore: current() reads the ELEMENT when detached (the close path stores 0)",
      "file": "src/scroll-restore.ts",
      "before": "    if (host.isConnected) liveScrollTop = host.scrollTop;\n    return liveScrollTop;",
      "after": "    return host.scrollTop;"
    },
    {
      "label": "scroll-restore: set() trusts its argument instead of reading back (a clamp goes unseen)",
      "file": "src/scroll-restore.ts",
      "before": "    host.scrollTop = top;\n    liveScrollTop = host.scrollTop;",
      "after": "    host.scrollTop = top;\n    liveScrollTop = top;"
    },
    {
      "label": "scroll-restore: the re-assert loop never starts (one write, clamped by the layout in force)",
      "file": "src/scroll-restore.ts",
      "before": "    if (target <= 0) return;",
      "after": "    if (true) return;"
    },
    {
      "label": "scroll-restore: the TOP gets defended too (a new search/sort fights the user)",
      "file": "src/scroll-restore.ts",
      "before": "    if (target <= 0) return;",
      "after": "    if (false) return;"
    },
    {
      "label": "scroll-restore: a pending restore ignores the user's gesture",
      "file": "src/scroll-restore.ts",
      "before": "      if (userTookOver || !host.isConnected) return;",
      "after": "      if (!host.isConnected) return;"
    },
    {
      "label": "scroll-restore: a gesture only FLAGS, leaving one frame to fire against it",
      "file": "src/scroll-restore.ts",
      "before": "    userTookOver = true;\n    cancel();",
      "after": "    userTookOver = true;"
    },
    {
      "label": "scroll-restore: gesture listeners drop capture (a card's stopPropagation hides the flick)",
      "file": "src/scroll-restore.ts",
      "before": "host.addEventListener(ev, yieldScroller, { passive: true, capture: true });",
      "after": "host.addEventListener(ev, yieldScroller, { passive: true });"
    },
    {
      "label": "scroll-restore: Space joins the scroll keys (disarms a restore on a key that moves nothing)",
      "file": "src/scroll-restore.ts",
      "before": "  \"End\",\n]);",
      "after": "  \"End\",\n  \" \",\n]);"
    },
    {
      "label": "scroll-restore: typing in the search field counts as a scroll gesture",
      "file": "src/scroll-restore.ts",
      "before": "if (!NATIVE_SCROLL_KEYS.has(key) || isTyping()) return;",
      "after": "if (!NATIVE_SCROLL_KEYS.has(key)) return;"
    },
    {
      "label": "scroll-restore: a new restore does not re-arm (one flick disables it for the session)",
      "file": "src/scroll-restore.ts",
      "before": "    cancel();\n    userTookOver = false;",
      "after": "    cancel();"
    },
    {
      "label": "scroll-restore: dispose() leaks the scroll listener onto a detached host",
      "file": "src/scroll-restore.ts",
      "before": "      host.removeEventListener(\"scroll\", onScroll);",
      "after": "      void 0;"
    },
    {
      "label": "safe-view: sensitiveKeyword falls back to the packaged default (writes a keyword the user's filter does not honour)",
      "file": "src/safe-view.ts",
      "before": "  return cfg.keywords.length ? (cfg.keywords[0] as string) : null;",
      "after": "  return cfg.keywords.length ? (cfg.keywords[0] as string) : SAFE_VIEW_DEFAULT_KEYWORDS;"
    },
    {
      "label": "safe-view: sensitiveKeyword writes the LAST keyword (parseKeywords' order stops being load-bearing)",
      "file": "src/safe-view.ts",
      "before": "return cfg.keywords.length ? (cfg.keywords[0] as string) : null;",
      "after": "return cfg.keywords.length ? (cfg.keywords[cfg.keywords.length - 1] as string) : null;"
    },
    {
      "label": "gallery-file: .gif joins VIDEO_EXTS (every animated thumbnail becomes a <video>)",
      "file": "src/gallery-file.ts",
      "before": "  \".mpeg\",\n]);",
      "after": "  \".mpeg\",\n  \".gif\",\n]);"
    },
    {
      "label": "gallery-file: SANDBOXED_TYPES admits the browse-only path root (ships write controls the server 400s)",
      "file": "src/gallery-file.ts",
      "before": "export const SANDBOXED_TYPES: readonly string[] = [\"input\", \"output\", \"temp\"];",
      "after": "export const SANDBOXED_TYPES: readonly string[] = [\"input\", \"output\", \"temp\", \"path\"];"
    },
    {
      "label": "gallery-file: joinAbs stops stripping a trailing slash (doubled separator in every ?path= URL)",
      "file": "src/gallery-file.ts",
      "before": "  const d = (dir || \"/\").replace(/\\/+$/, \"\");",
      "after": "  const d = dir || \"/\";"
    },
    {
      "label": "gallery-file: metaRows walks META_FIELDS backwards (display order stops being fixed)",
      "file": "src/gallery-file.ts",
      "before": "  for (const { key, label } of META_FIELDS) {",
      "after": "  for (const { key, label } of [...META_FIELDS].reverse()) {"
    },
    {
      "label": "gallery-file: metaRows drops FALSY values (a seed of 0 vanishes from the overlay)",
      "file": "src/gallery-file.ts",
      "before": "    if (v === undefined || v === null) continue;",
      "after": "    if (!v) continue;"
    },
    {
      "label": "gallery-file: metaRows keeps whitespace-only values (a bare 'Negative:' row whose Copy copies nothing)",
      "file": "src/gallery-file.ts",
      "before": "    if (!value.trim()) continue;",
      "after": "    if (false) continue;"
    },
    {
      "label": "gallery-file: createViewStore hard-codes one pack's key (the other pack's stored preference is orphaned)",
      "file": "src/gallery-file.ts",
      "before": "  const viewKey = `${namespace}:view`;",
      "after": "  const viewKey = \"comfyui-gallery-loader:view\";"
    },
    {
      "label": "gallery-file: the view whitelist goes truthy (a stale value reaches the listing as flat)",
      "file": "src/gallery-file.ts",
      "before": "          mode: localStorage.getItem(viewKey) === \"flat\" ? \"flat\" : \"folder\",",
      "after": "          mode: localStorage.getItem(viewKey) ? \"flat\" : \"folder\","
    },
    {
      "label": "gallery-file: recovery stops rewriting the preference (every open recovers again, flat unreachable)",
      "file": "src/gallery-file.ts",
      "before": "          localStorage.setItem(viewKey, \"folder\");",
      "after": "          void viewKey;"
    },
    {
      "label": "gallery-file: a throwing storage read escapes the store (private mode takes the modal down)",
      "file": "src/gallery-file.ts",
      "before": "        // Private mode / disabled storage — non-fatal.\n        return { mode: \"folder\", recovered: false };",
      "after": "        // Private mode / disabled storage — non-fatal.\n        throw new Error(\"storage unavailable\");"
    },
    {
      "label": "modal-fuzzy: highlightMatches CLAMPS out-of-range indices (marks a character the user never searched for)",
      "file": "src/modal-fuzzy.ts",
      "before": "  const set = new Set(matchIndices || []);",
      "after": "  const set = new Set((matchIndices || []).map((i) => Math.min(Math.max(i, 0), target.length - 1)));"
    },
    {
      "label": "modal-fuzzy: the highlight class is renamed (every pack's .cmp-match styling stops applying)",
      "file": "src/modal-fuzzy.ts",
      "before": "      m.className = \"cmp-match\";",
      "after": "      m.className = \"cmp-hit\";"
    },
    {
      "label": "modal-rating: applyStars only ADDS is-on (a failed write's rollback leaves the optimistic paint)",
      "file": "src/modal-rating.ts",
      "before": "    s.classList.toggle(\"is-on\", Number(s.dataset.val) <= r);",
      "after": "    if (Number(s.dataset.val) <= r) s.classList.add(\"is-on\");"
    },
    {
      "label": "modal-rating: applyStars writes the RAW rating to data-rating (attribute and lit stars drift)",
      "file": "src/modal-rating.ts",
      "before": "  row.dataset.rating = String(r);",
      "after": "  row.dataset.rating = String(rating);"
    },
    {
      "label": "modal-rating: applyStars queries the DOCUMENT (one card's rating repaints the whole grid)",
      "file": "src/modal-rating.ts",
      "before": "  for (const s of row.querySelectorAll<HTMLElement>(\"[data-val]\")) {",
      "after": "  for (const s of document.querySelectorAll<HTMLElement>(\"[data-val]\")) {"
    },
    {
      "label": "modal-rating: postRating treats {ok:false} as success (the caller never rolls back)",
      "file": "src/modal-rating.ts",
      "before": "  if (!data.ok) throw new Error(data.error || \"rating failed\");",
      "after": "  if (false) throw new Error(data.error || \"rating failed\");"
    },
    {
      "label": "modal-rating: postRating echoes the sent rating instead of the server's",
      "file": "src/modal-rating.ts",
      "before": "  return typeof data.rating === \"number\" ? data.rating : rating;",
      "after": "  return rating;"
    },
    {
      "label": "modal-rating: postRating ignores the HTTP status (a 500 resolves)",
      "file": "src/modal-rating.ts",
      "before": "  if (!res.ok) throw new Error(`HTTP ${res.status}`);",
      "after": "  if (false) throw new Error(`HTTP ${res.status}`);"
    },
    {
      "label": "CONTROL — comment text only; MUST be MISSED, or this harness is lying",
      "file": "src/hub.ts",
      "before": "the split between them is load-bearing",
      "after": "the split between them is decorative"
    }
  ]
}
